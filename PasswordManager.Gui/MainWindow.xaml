<Window x:Class="PasswordManager.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="pwman" Height="520" Width="480"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3A7CA5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="0,4,0,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Margin" Value="0,4,0,4"/>
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Margin" Value="0,4,0,4"/>
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">

        <!-- ===================== AUTH PANEL ===================== -->
        <StackPanel x:Name="AuthPanel" VerticalAlignment="Center">
            <TextBlock Text="pwman" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock x:Name="AuthModeLabel" Text="Enter your master password" Margin="0,0,0,8"/>
            <PasswordBox x:Name="MasterPasswordBox" KeyDown="MasterPasswordBox_KeyDown"/>

            <PasswordBox x:Name="ConfirmPasswordBox" Visibility="Collapsed"
                         PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>
            <TextBlock x:Name="ConfirmLabel" Text="Confirm master password" Visibility="Collapsed" Margin="0,4,0,0"/>

            <Button x:Name="AuthActionButton" Content="Unlock" Click="AuthActionButton_Click" Margin="0,16,0,0"/>

            <TextBlock x:Name="AuthStatusText" Foreground="#E06666" Margin="0,10,0,0"
                       TextWrapping="Wrap" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- ===================== VAULT PANEL ===================== -->
        <Grid x:Name="VaultPanel" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="Your vault" FontSize="20" FontWeight="Bold" DockPanel.Dock="Left"/>
                <Button Content="Lock" Click="LockButton_Click" DockPanel.Dock="Right"
                        HorizontalAlignment="Right" Background="#555555"/>
            </DockPanel>

            <ListBox x:Name="ServiceListBox" Grid.Row="1" Margin="0,0,0,12"/>

            <StackPanel Grid.Row="2">
                <UniformGrid Columns="2" Rows="2">
                    <Button Content="Add New" Click="AddButton_Click" Margin="0,4,4,4"/>
                    <Button Content="View" Click="ViewButton_Click" Margin="4,4,0,4"/>
                    <Button Content="Copy Password" Click="CopyPasswordButton_Click" Margin="0,4,4,4"/>
                    <Button Content="Delete" Click="DeleteButton_Click" Background="#B04A4A" Margin="4,4,0,4"/>
                </UniformGrid>
            </StackPanel>
        </Grid>

        <!-- ===================== ADD ENTRY OVERLAY ===================== -->
        <Border x:Name="AddOverlay" Background="#1E1E1E" Visibility="Collapsed"
                BorderBrush="#3A7CA5" BorderThickness="1" CornerRadius="4" Padding="16">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Add credential" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

                <TextBlock Text="Service (e.g. gmail)"/>
                <TextBox x:Name="NewServiceBox"/>

                <TextBlock Text="Username"/>
                <TextBox x:Name="NewUsernameBox"/>

                <CheckBox x:Name="GeneratePasswordCheck" Content="Generate a strong password for me"
                          Checked="GeneratePasswordCheck_Changed" Unchecked="GeneratePasswordCheck_Changed"
                          Margin="0,8,0,8"/>

                <TextBlock x:Name="NewPasswordLabel" Text="Password"/>
                <PasswordBox x:Name="NewPasswordBox"/>
                <TextBlock x:Name="PasswordStrengthText" Margin="0,4,0,0" TextWrapping="Wrap" FontSize="12"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Content="Cancel" Click="CancelAddButton_Click" Background="#555555" Margin="0,0,8,0"/>
                    <Button Content="Save" Click="SaveAddButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

    </Grid>
</Window>
